# Create configs/database/database.conf
mkdir -p configs/database
cat > configs/database/database.conf << 'EOF'
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_SCHEMA=openalex

# Connection pool settings
MAX_CONNECTIONS=100
MIN_CONNECTIONS=5
EOF